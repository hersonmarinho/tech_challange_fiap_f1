spring:
  application:
    name: horadoprato
  datasource:
    url: ${DB_URL} #jdbc:mysql://db:3306/meu_banco?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${DB_USER} #java_user
    password: ${DB_PASSWORD} #javapassword
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
#  sql:
#    init:
#      mode: always
#      data-locations: classpath:data.sql
#      continue-on-error: false